wsu.app.jwtSecret=leetcodeapi-jwt-secret
wsu.app.jwtExpirationMs=10000000

# database configuration
# modify these values to your local env
spring.r2dbc.ip=127.0.0.1
spring.r2dbc.port=3306
spring.r2dbc.username=root
spring.r2dbc.password=10183284
spring.r2dbc.dbname=mydb
spring.r2dbc.initialization-mode=always

# server configuration
server.port=8081

# Comma separated list of app that registered their own role in yml file (e.g. lctracker.yml)
app.role-registries=test